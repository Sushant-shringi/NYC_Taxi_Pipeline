version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
  build:
    commands:
      - echo "Creating deployment package..."
      # Hum hamari files ko artifacts folder mein bhej rahe hain bina kisi CLI command ke
artifacts:
  files:
    - src/taxi_extractor_lambda.py
    - src/taxi_transformer_lambda.py
    - src/taxi_loader_lambda.py